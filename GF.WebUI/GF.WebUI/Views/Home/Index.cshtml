
<div class="text-center">
    <div class="jumbotron jumbotron-fluid">
        <div class="container">
            @if (!User.Identity.IsAuthenticated)
            {
                <h1 class="display-4">High Rollers Welcome.</h1>
                <p class="lead">Sign up and get started.</p>

                <a class="btn btn-primary" asp-controller="account" asp-action="register">Make an account</a>
            }
            else
            {
                <h1 class="display-4">Welcome back!</h1>

                <a class="btn btn-primary" asp-controller="group" asp-action="grouplist">Find a group!</a>
                <p class="lead">OR</p>
                <a class="btn btn-primary" asp-controller="group" asp-action="creategroup">Make a group!</a>
            }
        </div>
    </div>
</div>
